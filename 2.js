// 2-Chain string methods to take the input " learn-javascript ", trim it, convert to 
//uppercase, and replace "-" with " "
const results = " learn-javascript "
  .trim()            
  .toUpperCase()     
  .replace("-", " "); 

console.log(results);
